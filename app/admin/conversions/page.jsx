import StubPage from '@/components/admin/StubPage';
import { IconConversions } from '@/components/admin/icons';
export default function AdminConversionsRoute() {
  return <StubPage title="Conversions" Icon={IconConversions} description="A focused view of every converted deal — install status, invoice, warranty — is coming soon. For now, converted leads are visible with the Converted filter on the Leads table." />;
}
