import './bot-console.css';

export const metadata = {
  title: 'Heseos Bot — WhatsApp AI Bot Platform',
  description: 'Self-service, white-labeled WhatsApp AI bots for growing businesses.',
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
  themeColor: '#0b1420',
};

export default function BotRootLayout({ children }) {
  return children;
}
